services:
  web:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app:Z
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Helps with Hot Module Replacement in Docker
